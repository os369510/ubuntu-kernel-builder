## Usage
```
docker pull os369510/ubuntu-kernel-builder  
./docker-run.sh --img os369510/ubuntu-kernel-builder --pkg ${ubuntu-package}.deb
or
./docker-run.sh --img os369510/ubuntu-kernel-builder --debug # for -it
```
